"use client";

import Image from "next/image";
import { ImagePlus, MessageCircleMore, Send } from "lucide-react";
import { useState } from "react";
import { useQuoteStore } from "@/stores/quote-store";

export function QuoteConversations() {
  const quotes = useQuoteStore((state) => state.quotes);
  const addMessage = useQuoteStore((state) => state.addMessage);
  const [selectedId, setSelectedId] = useState("");
  const [message, setMessage] = useState("");
  const [attachments, setAttachments] = useState<File[]>([]);
  const selected = quotes.find((quote) => quote.id === selectedId) ?? quotes[0];
  async function send() { if (!selected || (!message.trim() && !attachments.length)) return; await addMessage(selected.id, "customer", message.trim(), attachments); setMessage(""); setAttachments([]); }

  if (!quotes.length) return <div className="rounded-[28px] bg-[#fffdfb] p-10 text-center"><MessageCircleMore className="mx-auto text-[#9e5f72]" /><h1 className="mt-4 font-display text-4xl font-semibold">Aún no tienes conversaciones</h1><p className="mt-3 text-sm text-[#786970]">Envía una solicitud personalizada y podrás continuarla aquí.</p></div>;
  return <div className="grid min-h-[620px] overflow-hidden rounded-[28px] bg-[#fffdfb] lg:grid-cols-[280px_1fr]"><aside className="border-b border-[#e5d8dc] lg:border-b-0 lg:border-r"><p className="p-5 text-[10px] font-bold uppercase tracking-wider text-[#9e5f72]">Conversaciones</p>{quotes.map((quote) => <button key={quote.id} onClick={() => setSelectedId(quote.id)} className={`w-full border-t border-[#eee5e7] p-5 text-left ${selected?.id === quote.id ? "bg-[#f3e7e9]" : "hover:bg-[#faf6f6]"}`}><span className="block text-xs font-bold">J&J Estudio</span><span className="mt-1 block truncate text-[10px] text-[#786970]">{quote.description}</span><span className="mt-2 block text-[9px] text-[#9e5f72]">{quote.status}</span></button>)}</aside>{selected && <section className="flex min-h-0 flex-col"><div className="border-b border-[#e5d8dc] p-6"><p className="text-xs font-bold">Conversación con J&J Estudio</p><p className="mt-1 text-[10px] text-[#786970]">Tu solicitud: {selected.dimensions} · {selected.quantity} unidad(es) · {selected.color}</p></div><div className="flex-1 space-y-3 overflow-y-auto p-6">{selected.images.length > 0 && <div className="grid grid-cols-4 gap-2">{selected.images.map((image) => <div key={image.id} className="relative aspect-square overflow-hidden rounded-xl"><Image src={image.url} alt={image.name} fill unoptimized className="object-cover" /></div>)}</div>}{selected.messages.map((item) => <div key={item.id} className={`max-w-[85%] rounded-2xl px-4 py-3 text-xs ${item.sender === "customer" ? "ml-auto bg-[#35282d] text-white" : "bg-[#f3e7e9]"}`}><p>{item.text}</p>{item.images?.length ? <div className="mt-2 grid grid-cols-3 gap-2">{item.images.map((image) => <a key={image.id} href={image.url} target="_blank" rel="noreferrer" className="relative aspect-square overflow-hidden rounded-lg"><Image src={image.url} alt={image.name} fill unoptimized className="object-cover" /></a>)}</div> : null}<span className="mt-1 block text-[8px] opacity-60">{item.createdAt}</span></div>)}</div><div className="border-t border-[#e5d8dc] p-4"><div className="flex gap-2"><input value={message} onChange={(event) => setMessage(event.target.value)} onKeyDown={(event) => event.key === "Enter" && void send()} className="min-w-0 flex-1 rounded-full border border-[#d8c9cd] px-4 py-3 text-sm" placeholder="Escribe un mensaje" /><button onClick={() => void send()} className="grid size-11 place-items-center rounded-full bg-[#35282d] text-white" aria-label="Enviar"><Send size={16} /></button></div><label className="mt-3 inline-flex cursor-pointer items-center gap-2 rounded-full border border-[#d8c9cd] px-3 py-2 text-[9px] font-bold text-[#9e5f72]"><ImagePlus size={13} /> Adjuntar imágenes<input type="file" accept="image/*" multiple className="sr-only" onChange={(event) => setAttachments(Array.from(event.target.files ?? []).filter((file) => file.size <= 8 * 1024 * 1024).slice(0, 4))} /></label>{attachments.length > 0 && <span className="ml-2 text-[9px] text-[#786970]">{attachments.length} imagen(es) listas para enviar</span>}</div></section>}</div>;
}
